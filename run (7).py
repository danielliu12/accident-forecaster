"""
Indiana Accident Forecaster — SELF-CONTAINED single-file build.
Writes its own data and module files at startup, then runs the full
pipeline. Requires only: pip install pandas numpy requests
Usage: python run.py --hours 48
"""
import os, argparse

os.makedirs('data', exist_ok=True)
os.makedirs('output', exist_ok=True)

_FILES = {}
_FILES['data/county_opportunity.csv'] = 'rank,county,opportunity_score,weighted_score,injured_patients,serious_cases,viability,pct_non_english\n1,Marion,32917.5,21945.0,11458.7,1289.3,1.0,18.2\n2,Allen,6531.3,5182.0,3035.3,121.0,0.7603767003836763,12.4\n3,Lake,5591.1,3859.3,2152.0,224.7,0.9487268922218345,15.1\n4,Hamilton,3751.7,2960.3,1772.0,134.3,0.7673526334147192,12.5\n5,Elkhart,3550.0,2366.7,1319.0,74.0,1.0,23.0\n6,St. Joseph,2796.7,2391.0,1280.7,75.3,0.6696895709801185,11.1\n7,Tippecanoe,2604.3,1842.0,993.7,102.3,0.91384722706662,14.6\n8,Hendricks,2308.3,2072.3,1223.3,107.3,0.6138821067317753,10.3\n9,Vanderburgh,1709.1,2409.7,1337.7,76.0,0.2092779909312871,4.5\n10,Bartholomew,1607.5,1137.0,673.3,52.0,0.91384722706662,14.6\n11,Johnson,1569.7,1611.0,894.3,50.7,0.4743634461109174,8.3\n12,Delaware,1428.8,2118.7,1159.3,115.3,0.1743983257760725,4.0\n13,Porter,1321.9,1461.3,881.3,55.3,0.4046041158004883,7.3\n14,Madison,1295.2,1773.7,1014.3,57.7,0.2302057900244157,4.8\n15,Kosciusko,1267.7,984.0,583.0,40.0,0.788280432507848,12.8\n16,Monroe,997.4,837.7,407.7,69.7,0.6906173700732473,11.4\n17,Hancock,970.5,1439.0,850.3,71.7,0.1743983257760725,4.0\n18,Noble,937.6,629.0,397.3,3.0,0.990582490408092,15.7\n19,Vigo,916.9,1255.7,644.3,50.3,0.2302057900244157,4.8\n20,Cass,909.9,637.3,375.3,32.3,0.927799093128706,14.8\n21,Boone,880.8,981.3,603.3,29.7,0.3976281827694454,7.2\n22,Lagrange,848.6,565.7,349.0,15.0,1.0,48.1\n23,Marshall,783.0,535.3,306.0,18.7,0.9626787582839205,15.3\n24,Jackson,730.9,576.7,320.0,36.7,0.7673526334147192,12.5\n25,Laporte,714.7,909.3,488.0,61.0,0.2860132542727589,5.6\n26,Daviess,691.0,460.7,305.3,1.7,1.0,19.4\n27,Clinton,690.2,550.7,311.0,35.3,0.7534007673526334,12.3\n28,Wayne,671.4,832.0,457.7,32.0,0.3069410533658877,5.9\n29,Howard,659.2,1053.7,624.3,40.3,0.1255667945587722,3.3\n30,Morgan,543.2,977.3,543.7,60.7,0.05580746424834321,2.3\n31,Dubois,518.1,528.0,341.7,11.3,0.4813393791419602,8.4\n32,Shelby,462.7,594.0,381.7,20.0,0.279037321241716,5.5\n33,White,439.9,448.3,235.7,29.7,0.4813393791419602,8.4\n34,Adams,407.5,271.7,147.0,17.3,1.0,16.0\n35,Grant,398.3,596.7,316.0,37.3,0.1674223927450296,3.9\n36,Harrison,396.8,705.0,460.0,11.7,0.0627833972793861,2.4\n37,Jasper,370.8,484.7,272.7,32.0,0.2650854551796302,5.3\n38,Putnam,369.0,558.7,361.3,19.7,0.1604464597139867,3.8\n39,Clark,366.5,439.0,248.7,9.3,0.3348447854900592,6.3\n40,Henry,366.1,658.7,376.3,38.7,0.05580746424834321,2.3\n41,Gibson,352.1,625.7,437.7,4.0,0.0627833972793861,2.4\n42,Warrick,350.7,567.0,342.0,4.3,0.1185908615277293,3.2\n43,Dekalb,349.8,614.0,386.3,4.3,0.06975933031042901,2.5\n44,Huntington,299.3,512.7,318.7,4.7,0.08371119637251483,2.7\n45,Floyd,296.9,464.3,273.0,15.0,0.139518660620858,3.5\n46,Whitley,288.3,518.7,349.0,1.0,0.05580746424834321,2.3\n47,Montgomery,280.4,338.7,183.7,19.0,0.3278688524590164,6.2\n48,Knox,278.5,413.0,207.7,27.7,0.1743983257760725,4.0\n49,Dearborn,263.8,520.3,280.3,29.3,0.006975933031042908,1.6\n50,Scott,261.4,488.7,258.0,39.7,0.03487966515521451,2.0\n51,Miami,234.9,371.3,210.0,32.0,0.1325427275898151,3.4\n52,Ripley,215.5,419.3,261.7,17.7,0.0139518660620858,1.7\n53,Lawrence,213.6,410.0,203.0,36.0,0.02092779909312871,1.8\n54,Randolph,210.6,283.0,152.7,18.3,0.2441576560865016,5.0\n55,Wabash,207.3,377.7,222.7,17.7,0.04883153121730032,2.2\n56,Steuben,201.6,315.3,177.0,21.0,0.139518660620858,3.5\n57,Greene,201.5,403.0,217.3,20.3,0.0,1.4\n58,Fulton,195.0,250.3,147.0,10.7,0.279037321241716,5.5\n59,Jay,187.9,214.3,129.0,6.7,0.3767003836763167,6.9\n60,Fountain,187.1,313.0,175.0,21.3,0.09766306243460061,2.9\n61,Parke,178.0,208.0,120.0,11.0,0.3557725845831879,6.6\n62,Jefferson,173.6,265.7,142.3,24.0,0.1534705266829438,3.7\n63,Wells,169.4,277.0,175.3,9.3,0.1116149284966864,3.1\n64,Spencer,160.5,251.0,153.3,9.7,0.139518660620858,3.5\n65,Washington,158.4,304.0,161.0,31.7,0.02092779909312871,1.8\n66,Franklin,154.2,261.0,145.0,22.0,0.0906871294035577,2.8\n67,Jennings,142.6,223.0,126.0,18.3,0.139518660620858,3.5\n68,Owen,135.0,246.0,143.7,20.0,0.04883153121730032,2.2\n69,Rush,119.8,183.3,92.7,20.3,0.1534705266829438,3.7\n70,Pulaski,116.5,188.3,98.3,18.0,0.1185908615277293,3.2\n71,Tipton,111.4,184.3,103.7,6.7,0.1046389954656435,3.0\n72,Posey,111.2,222.3,128.0,9.0,0.0,1.2\n73,Carroll,109.5,171.3,105.3,11.3,0.139518660620858,3.5\n74,Sullivan,102.4,204.7,107.0,9.3,0.0,1.5\n75,Vermillion,100.2,200.3,107.7,13.7,0.0,1.5\n76,Clay,93.8,187.7,102.7,15.7,0.0,1.5\n77,Fayette,91.8,171.7,83.3,11.0,0.03487966515521451,2.0\n78,Orange,85.6,145.0,72.3,13.7,0.0906871294035577,2.8\n79,Newton,81.4,101.7,57.7,4.0,0.2999651203348447,5.8\n80,Martin,75.7,147.3,83.0,7.7,0.0139518660620858,1.7\n81,Starke,75.4,124.7,70.0,8.3,0.1046389954656435,3.0\n82,Benton,74.6,99.3,55.3,4.7,0.2511335891175444,5.1\n83,Decatur,71.0,121.7,69.0,9.0,0.08371119637251483,2.7\n84,Brown,69.4,123.3,63.3,11.3,0.0627833972793861,2.4\n85,Switzerland,67.3,88.0,44.0,8.3,0.2650854551796302,5.3\n86,Crawford,51.1,89.7,43.7,5.0,0.06975933031042901,2.5\n87,Pike,50.5,101.0,67.0,0.7,0.0,0.5\n88,Warren,47.8,88.3,47.7,9.3,0.04185559818625741,2.1\n89,Perry,41.3,78.3,42.7,4.7,0.0279037321241716,1.9\n90,Union,33.0,59.3,32.7,3.3,0.05580746424834321,2.3\n91,Ohio,28.3,45.7,23.3,4.3,0.1185908615277293,3.2\n92,Blackford,22.9,39.3,18.7,4.3,0.08371119637251483,2.7\n'
_FILES['data/phase2_metro_priority.csv'] = 'metro,crash_weighted_score,injured_patients_yr,serious_cases_yr,search_demand_mo,crash_n,search_n,priority_score\nindianapolis,32580.199999999997,17727.300000000003,1763.6999999999998,4550.0,1.0,1.0,100.0\nfortwayne,7376.099999999999,4411.6,157.60000000000002,2550.0,0.22639824187696822,0.5604395604395604,35.6\nnwindiana,6816.3,3851.7,377.0,2200.0,0.2092160269120509,0.4835164835164835,31.8\nsouthbend,6277.0,3488.7,208.0,1650.0,0.1926630284651414,0.3626373626373626,26.4\nlafayette,3111.6000000000004,1701.0,183.29999999999998,1550.0,0.09550585938698966,0.34065934065934067,18.0\nevansville,3824.7,2245.4,93.3,1150.0,0.11739338616705852,0.25274725274725274,17.2\n'
_FILES['data/phase2_cluster_metro_efficiency.csv'] = 'metro,cluster,monthly_searches,est_cpc,searches_per_1000usd\nevansville,commercial_truck,200.0,265.18,754.0\nevansville,core_auto,800.0,337.77,2368.0\nevansville,motorcycle,50.0,392.31,127.0\nevansville,pre_lawyer_questions,50.0,15.0,3333.0\nevansville,specialty_auto,50.0,337.77,148.0\nfortwayne,commercial_truck,200.0,265.18,754.0\nfortwayne,core_auto,2200.0,337.77,6513.0\nfortwayne,motorcycle,100.0,392.31,255.0\nfortwayne,pre_lawyer_questions,0.0,15.0,0.0\nfortwayne,specialty_auto,50.0,337.77,148.0\nindianapolis,commercial_truck,650.0,265.18,2451.0\nindianapolis,core_auto,3600.0,337.77,10658.0\nindianapolis,motorcycle,100.0,392.31,255.0\nindianapolis,pre_lawyer_questions,150.0,15.0,10000.0\nindianapolis,specialty_auto,50.0,337.77,148.0\nlafayette,commercial_truck,150.0,265.18,566.0\nlafayette,core_auto,1250.0,337.77,3701.0\nlafayette,motorcycle,50.0,392.31,127.0\nlafayette,pre_lawyer_questions,50.0,15.0,3333.0\nlafayette,specialty_auto,50.0,337.77,148.0\nnwindiana,commercial_truck,200.0,265.18,754.0\nnwindiana,core_auto,1750.0,337.77,5181.0\nnwindiana,motorcycle,50.0,392.31,127.0\nnwindiana,pre_lawyer_questions,100.0,15.0,6667.0\nnwindiana,specialty_auto,100.0,337.77,296.0\nsouthbend,commercial_truck,200.0,265.18,754.0\nsouthbend,core_auto,1250.0,337.77,3701.0\nsouthbend,motorcycle,100.0,392.31,255.0\nsouthbend,pre_lawyer_questions,50.0,15.0,3333.0\nsouthbend,specialty_auto,50.0,337.77,148.0\n'
_FILES['config.py'] = '"""\nConfiguration — Indiana Accident Forecaster v2 (county-based).\n\nSpatial unit: county. The baseline layer is EMS-derived motor-vehicle\ninjury opportunity (data/county_opportunity.csv, built from Indiana Data\nHub EMS incidents with severity weighting). The top DYNAMIC_N counties by\nopportunity get live weather + incident monitoring; the long tail stays at\nits static baseline.\n"""\n\nCOUNTY_CSV = "data/county_opportunity.csv"\nPHASE2_PRIORITY_CSV = "data/phase2_metro_priority.csv"\nPHASE2_EFFICIENCY_CSV = "data/phase2_cluster_metro_efficiency.csv"\n\nDYNAMIC_N = 25  # counties with live weather/incidents (~84% of opportunity)\n\n# Approximate county centroids for the dynamic tier (lat, lon). Used only\n# for weather sampling and nearest-county incident assignment; ±10-20 km\n# error is immaterial at forecast scale.\nCOUNTY_CENTROIDS = {\n    "Marion": (39.78, -86.14), "Allen": (41.09, -85.07),\n    "Lake": (41.42, -87.37), "Hamilton": (40.07, -86.05),\n    "Elkhart": (41.60, -85.87), "St. Joseph": (41.62, -86.29),\n    "Tippecanoe": (40.39, -86.89), "Hendricks": (39.77, -86.51),\n    "Vanderburgh": (38.02, -87.59), "Bartholomew": (39.20, -85.90),\n    "Johnson": (39.49, -86.10), "Delaware": (40.23, -85.40),\n    "Porter": (41.46, -87.07), "Madison": (40.16, -85.72),\n    "Kosciusko": (41.24, -85.86), "Monroe": (39.16, -86.52),\n    "Hancock": (39.82, -85.77), "Noble": (41.40, -85.42),\n    "Vigo": (39.43, -87.39), "Cass": (40.76, -86.35),\n    "Boone": (40.05, -86.47), "Lagrange": (41.64, -85.43),\n    "Marshall": (41.32, -86.26), "Jackson": (38.91, -86.04),\n    "Laporte": (41.55, -86.74),\n}\n\n# County -> Phase 2 metro mapping (for blending with the static search-\n# economics shares). Counties not listed roll into "other_in".\nCOUNTY_TO_METRO = {\n    "Marion": "indianapolis", "Hamilton": "indianapolis",\n    "Hendricks": "indianapolis", "Johnson": "indianapolis",\n    "Boone": "indianapolis", "Hancock": "indianapolis",\n    "Madison": "indianapolis", "Morgan": "indianapolis",\n    "Shelby": "indianapolis",\n    "Allen": "fortwayne", "Whitley": "fortwayne", "Wells": "fortwayne",\n    "Noble": "fortwayne", "Dekalb": "fortwayne", "Huntington": "fortwayne",\n    "Lake": "nwindiana", "Porter": "nwindiana", "Laporte": "nwindiana",\n    "Jasper": "nwindiana", "Newton": "nwindiana",\n    "St. Joseph": "southbend", "Elkhart": "southbend",\n    "Marshall": "southbend", "Kosciusko": "southbend",\n    "Lagrange": "southbend",\n    "Tippecanoe": "lafayette", "Clinton": "lafayette",\n    "White": "lafayette", "Carroll": "lafayette",\n    "Vanderburgh": "evansville", "Warrick": "evansville",\n    "Posey": "evansville", "Gibson": "evansville",\n}\n\nOPEN_METEO_URL = "https://api.open-meteo.com/v1/forecast"\n\n# --- 511in.org live incidents ---------------------------------------------\n# Primary: the same GraphQL endpoint the public 511in.org map uses (verified\n# via browser DevTools, July 2026). Anonymous requests are attempted first;\n# the site\'s own map issues short-lived visitor tokens, so nothing is\n# hardcoded here. Statewide bbox; server-side clusters are drilled into by\n# re-querying each cluster\'s bbox at its maxZoom+1 (bounded by\n# GRAPHQL_MAX_REQUESTS per run).\nGRAPHQL_URL = "https://511in.org/api/graphql"\nGRAPHQL_LAYERS = ["incidents"]\nSTATE_BBOX = {"north": 41.95, "south": 37.65, "east": -84.55, "west": -88.25}\nGRAPHQL_MAX_REQUESTS = 25\n\n# Legacy fallbacks (kept for graceful degradation; the api/v2 endpoint is\n# the IBI-platform standard used by other states and is NOT expected to\n# work for Indiana — see [warn] lines in logs).\nTRAFFICWISE_EVENTS_URLS = [\n    "https://511in.org/api/v2/get/event?format=json",\n    "https://content.trafficwise.org/json/cars.json",\n]\nTRAFFICWISE_API_KEY = None  # legacy; unused unless api/v2 ever works\n\n# --- TomTom Traffic Incidents (local-road coverage) ------------------------\n# Blended with 511. 511\'s real gap is geography (INDOT-managed roads only);\n# TomTom\'s probe data adds local-road incidents statewide. Requires a free\n# TomTom developer key stored as the TOMTOM_API_KEY GitHub Actions secret\n# (the workflow exports it as an env var). If the env var is absent the\n# feed is skipped and everything runs exactly as before.\nTOMTOM_URL = "https://api.tomtom.com/traffic/services/5/incidentDetails"\nTOMTOM_TILE_DEG = 1.0      # API bbox limit is 10,000 km^2 (~1x1 degree here)\nTOMTOM_MAX_REQUESTS = 24   # per run; 20 tiles cover the statewide bbox\nTOMTOM_DEDUP_KM = 1.5      # drop TomTom events this close to a 511 event\n\n# If the GitHub Actions poller is set up, its raw incident-history URL,\n# e.g. "https://raw.githubusercontent.com/USER/REPO/main/incident_history.json"\n# Colab runs will merge this machine-built archive into local memory.\nINCIDENT_LOG_URL = None\n\n# Weather multipliers (FHWA road-weather program; Qiu & Nixon 2008;\n# Theofilatos & Yannis 2014 — conservative midpoints).\nWEATHER_MULTIPLIERS = {\n    "rain_light": 1.25, "rain_moderate": 1.5, "rain_heavy": 1.8,\n    "snow_light": 1.5, "snow_moderate": 1.8, "snow_heavy": 2.2,\n    "freezing_precip": 2.5, "fog": 1.4, "darkness": 1.25, "high_wind": 1.15,\n}\nMAX_MULTIPLIER = 3.5\nINCIDENT_BOOST_PER_EVENT = 0.10   # +10% county risk per unit of\n#   severity-weighted live activity: crash=1.0, closure/hazard=0.6,\n#   stalled=0.4, congestion=0.3, roadwork=0.15, \'reopened\'=ignored.\n#   See EVENT_WEIGHTS in sources.py.\n# Collisions vs contributing factors: only events with weight >= this get\n# a map marker. 1.0 = crashes only (incl. "road closed due to crash",\n# which classifies as crash). Lower to 0.6 to also mark hazards (ice,\n# flooding, spills). Everything below the bar still shades its county\'s\n# color and is itemized in the county click-panel.\nMAP_MIN_MARKER_WEIGHT = 1.0\n# Events below this weight never enter trailing memory (roadwork and\n# congestion have near-zero trailing signal but would bloat\n# incident_history.json now that TomTom reports them statewide).\nMEMORY_MIN_WEIGHT = 0.4\n\nTRAILING_BOOST_PER_POINT = 0.05   # +5% per point of decayed trailing weight\nINCIDENT_BOOST_CAP = 1.75         # cap on combined live + trailing boost\nTRAILING_HALF_LIFE_H = 48         # trailing importance halves every 48 h\n\n# Hour-of-week exposure fallback (INDOT diurnal shape). Upgrade path: learn\n# this empirically from EMS run timestamps if a filtered extract with a\n# datetime column is provided.\nWEEKDAY_HOURLY = [0.25,0.15,0.12,0.12,0.25,0.6,1.3,1.9,1.6,1.2,1.15,1.25,\n                  1.35,1.35,1.5,1.8,2.0,2.1,1.6,1.1,0.85,0.7,0.55,0.4]\nWEEKEND_HOURLY = [0.45,0.3,0.25,0.15,0.15,0.25,0.45,0.7,1.0,1.3,1.5,1.6,\n                  1.65,1.6,1.55,1.5,1.45,1.4,1.3,1.1,0.95,0.8,0.7,0.55]\n\n# Budget blending: forecast may move up to this fraction of allocation away\n# from the static Phase 2 shares.\nDYNAMIC_WEIGHT = 0.35\nSTATIC_SHARES = {\n    "indianapolis": 0.50, "fortwayne": 0.15, "nwindiana": 0.13,\n    "southbend": 0.10, "lafayette": 0.06, "evansville": 0.06,\n}\n'
_FILES['sources.py'] = '"""\nData connectors (v2): county opportunity table, batched Open-Meteo\nforecasts for the dynamic counties, 511/CARS live incidents.\n\nAll network connectors degrade gracefully to neutral values so a\nscheduled run always completes.\n"""\nfrom __future__ import annotations\n\nimport datetime as dt\nimport math\nimport os\nimport sys\n\nimport pandas as pd\nimport requests\n\nimport config\n\n\ndef load_counties() -> pd.DataFrame:\n    df = pd.read_csv(config.COUNTY_CSV)\n    df["county"] = df["county"].astype(str).str.strip()\n    df["dynamic"] = False\n    dyn = df.nlargest(config.DYNAMIC_N, "opportunity_score")["county"]\n    df.loc[df["county"].isin(dyn), "dynamic"] = True\n    df["metro"] = df["county"].map(config.COUNTY_TO_METRO).fillna("other_in")\n    return df\n\n\n# ---------------------------------------------------------------------------\n# Weather — one batched request for all dynamic counties\n# ---------------------------------------------------------------------------\nHOURLY_VARS = ["precipitation", "snowfall", "temperature_2m", "visibility",\n               "wind_gusts_10m", "is_day", "weather_code"]\n\n\ndef fetch_weather(hours: int = 48) -> dict[str, pd.DataFrame]:\n    names = list(config.COUNTY_CENTROIDS)\n    lats = ",".join(str(config.COUNTY_CENTROIDS[n][0]) for n in names)\n    lons = ",".join(str(config.COUNTY_CENTROIDS[n][1]) for n in names)\n    params = {\n        "latitude": lats, "longitude": lons,\n        "hourly": ",".join(HOURLY_VARS),\n        "forecast_days": min(7, max(1, math.ceil(hours / 24))),\n        "timezone": "America/Indiana/Indianapolis",\n    }\n    out = {}\n    try:\n        r = requests.get(config.OPEN_METEO_URL, params=params, timeout=30)\n        r.raise_for_status()\n        payload = r.json()\n        if isinstance(payload, dict):  # single-location fallback\n            payload = [payload]\n        for name, loc in zip(names, payload):\n            df = pd.DataFrame(loc["hourly"])\n            df["time"] = pd.to_datetime(df["time"])\n            df = df.set_index("time").iloc[:hours]\n            df["freezing_precip_flag"] = df["weather_code"].isin(\n                [56, 57, 66, 67])\n            out[name] = df\n    except Exception as e:  # noqa: BLE001\n        print(f"[warn] weather fetch failed ({e}); using neutral weather.",\n              file=sys.stderr)\n    for name in names:  # fill any gaps\n        out.setdefault(name, neutral_weather(hours))\n    return out\n\n\ndef neutral_weather(hours: int) -> pd.DataFrame:\n    idx = pd.date_range(dt.datetime.now().replace(minute=0, second=0,\n                                                  microsecond=0),\n                        periods=hours, freq="h")\n    return pd.DataFrame({\n        "precipitation": 0.0, "snowfall": 0.0, "temperature_2m": 15.0,\n        "visibility": 20000.0, "wind_gusts_10m": 10.0, "is_day": 1,\n        "weather_code": 0, "freezing_precip_flag": False,\n    }, index=idx)\n\n\ndef weather_multiplier(row: pd.Series) -> float:\n    m = config.WEATHER_MULTIPLIERS\n    mult = 1.0\n    snow = row.get("snowfall", 0.0)        # cm/h\n    rain = row.get("precipitation", 0.0)   # mm/h\n    if row.get("freezing_precip_flag", False) or (\n            rain > 0.1 and row.get("temperature_2m", 15) <= 0):\n        mult *= m["freezing_precip"]\n    elif snow > 2.5:\n        mult *= m["snow_heavy"]\n    elif snow > 1.0:\n        mult *= m["snow_moderate"]\n    elif snow > 0.1:\n        mult *= m["snow_light"]\n    elif rain > 7.6:\n        mult *= m["rain_heavy"]\n    elif rain > 2.5:\n        mult *= m["rain_moderate"]\n    elif rain > 0.1:\n        mult *= m["rain_light"]\n    if row.get("visibility", 20000) < 1000:\n        mult *= m["fog"]\n    if not row.get("is_day", 1):\n        mult *= m["darkness"]\n    if row.get("wind_gusts_10m", 0) > 40:\n        mult *= m["high_wind"]\n    return min(mult, config.MAX_MULTIPLIER)\n\n\n# ---------------------------------------------------------------------------\n# Live incidents -> per-county active event counts\n# ---------------------------------------------------------------------------\n# Substance-based event classification. Order matters: first match wins.\n# weight 0.0 = not a risk signal at all (dropped entirely: no boost, no\n# marker, no memory).\nEVENT_WEIGHTS = [\n    ("reopened/cleared", 0.0, (\n        "back open", "reopened", "re-opened", "all lanes open",\n        "has been cleared", "now open", "open to traffic", "has ended")),\n    ("crash", 1.0, (\n        "crash", "accident", "collision", "overturned", "jackknif",\n        "pileup", "multi vehicle", "multi-vehicle", "rollover")),\n    ("stalled/disabled", 0.4, (\n        "stalled", "disabled vehicle", "vehicle fire", "abandoned vehicle")),\n    ("closure/hazard", 0.6, (\n        "road closed", "ramp closed", "bridge closed", "closed in both",\n        "closure", "hazmat", "spill", "debris", "flood", "high water",\n        "fire", "downed", "ice", "snow covered")),\n    ("congestion", 0.3, (\n        "slow traffic", "congestion", "delay", "stopped traffic",\n        "heavy traffic", "backup")),\n    ("roadwork", 0.15, (\n        "construction", "roadwork", "road work", "maintenance",\n        "restriction", "lane closed", "lane closure", "paving",\n        "bridge work", "mowing", "utility work", "shoulder work")),\n]\nDEFAULT_EVENT_CATEGORY = ("other", 0.3)\n\n\ndef classify_event(text: str) -> tuple[str, float]:\n    """(category, risk weight) from an event\'s title + description."""\n    t = str(text).lower()\n    for cat, w, kws in EVENT_WEIGHTS:\n        if any(k in t for k in kws):\n            return cat, w\n    return DEFAULT_EVENT_CATEGORY\n\n\ndef _norm_bbox(cb):\n    """Cluster bbox -> (north, south, east, west); tolerates dict or\n    GeoJSON-style [west, south, east, north] list."""\n    if isinstance(cb, dict):\n        return (float(cb["north"]), float(cb["south"]),\n                float(cb["east"]), float(cb["west"]))\n    w, s, e, n = (float(x) for x in cb)\n    return (n, s, e, w)\n\n\ndef _feature_point(mf):\n    """First usable (lat, lon) from a mapFeature\'s GeoJSON geometries."""\n    for feat in (mf.get("features") or []):\n        g = feat.get("geometry") or {}\n        t, c = g.get("type"), g.get("coordinates")\n        if not c:\n            continue\n        if t == "Point":\n            return (float(c[1]), float(c[0]))\n        if t in ("MultiPoint", "LineString"):\n            return (float(c[0][1]), float(c[0][0]))\n        if t in ("MultiLineString", "Polygon"):\n            return (float(c[0][0][1]), float(c[0][0][0]))\n    return None\n\n\ndef _graphql_map_features(session, bbox, zoom):\n    """One MapFeatures query against the 511in.org GraphQL endpoint."""\n    payload = {\n        "query": (\n            "query MapFeatures($input: MapFeaturesArgs!) {"\n            " mapFeaturesQuery(input: $input) { mapFeatures {"\n            " bbox title tooltip uri"\n            " features { id geometry properties }"\n            " ... on Cluster { maxZoom } ... on Event { priority }"\n            " __typename } error { message type } } }"),\n        "variables": {"input": {\n            "north": bbox[0], "south": bbox[1],\n            "east": bbox[2], "west": bbox[3],\n            "zoom": zoom, "layerSlugs": config.GRAPHQL_LAYERS,\n            "nonClusterableUris": ["dashboard"],\n        }},\n    }\n    r = session.post(config.GRAPHQL_URL, json=payload, timeout=20)\n    r.raise_for_status()\n    data = r.json()\n    if data.get("errors"):\n        raise RuntimeError(str(data["errors"])[:200])\n    q = (data.get("data") or {}).get("mapFeaturesQuery") or {}\n    if q.get("error"):\n        raise RuntimeError(str(q["error"])[:200])\n    return q.get("mapFeatures") or []\n\n\ndef _fetch_incidents_graphql():\n    """Statewide incident list from the 511in.org public-map GraphQL API.\n    Anonymous request (the same data the public map shows). Clusters are\n    drilled into recursively via their bbox/maxZoom."""\n    session = requests.Session()\n    session.headers.update({\n        "User-Agent": "Mozilla/5.0",\n        "Origin": "https://511in.org",\n        "Referer": "https://511in.org/",\n        "language": "en",\n    })\n    events, seen = [], set()\n    budget = [config.GRAPHQL_MAX_REQUESTS]\n\n    def visit(bbox, zoom):\n        if budget[0] <= 0:\n            return\n        budget[0] -= 1\n        for mf in _graphql_map_features(session, bbox, zoom):\n            if mf.get("__typename") == "Cluster":\n                cb = mf.get("bbox")\n                try:\n                    nz = min(int(mf.get("maxZoom") or zoom + 2) + 1, 15)\n                except (TypeError, ValueError):\n                    nz = min(zoom + 2, 15)\n                if cb and nz > zoom:\n                    visit(_norm_bbox(cb), nz)\n                continue\n            uri = mf.get("uri") or ""\n            if uri and uri in seen:\n                continue\n            seen.add(uri)\n            pt = _feature_point(mf)\n            if pt is None:\n                continue\n            tooltip = str(mf.get("tooltip") or "")\n            title = str(mf.get("title") or "event")\n            cat, w = classify_event(title + " " + tooltip)\n            if w <= 0:\n                continue  # e.g. "road back open" — not a risk signal\n            events.append({\n                "id": uri or None,\n                "lat": pt[0], "lon": pt[1],\n                "type": title[:80],\n                "desc": tooltip[:160],\n                "category": cat, "weight": w,\n                "severity": None,\n                "full_closure": "closed" in tooltip.lower(),\n            })\n\n    visit((config.STATE_BBOX["north"], config.STATE_BBOX["south"],\n           config.STATE_BBOX["east"], config.STATE_BBOX["west"]), 11)\n    return events\n\n\ndef _fetch_incidents_legacy():\n    """Old IBI-style JSON endpoints, kept as a fallback."""\n    for url in config.TRAFFICWISE_EVENTS_URLS:\n        if config.TRAFFICWISE_API_KEY and "api/v2" in url:\n            url += "&key=" + config.TRAFFICWISE_API_KEY\n        try:\n            r = requests.get(url, timeout=20,\n                             headers={"User-Agent": "Mozilla/5.0"})\n            r.raise_for_status()\n            raw = r.json()\n            items = raw if isinstance(raw, list) else raw.get("events", [])\n        except Exception as e:  # noqa: BLE001\n            print(f"[warn] legacy event feed failed: {url.split(\'?\')[0]} "\n                  f"({e})", file=sys.stderr)\n            continue\n        events = []\n        for ev in items:\n            lat = ev.get("latitude") or ev.get("lat") or ev.get("Latitude")\n            lon = ev.get("longitude") or ev.get("lon") or ev.get("Longitude")\n            if lat is None or lon is None:\n                continue\n            etype = str(ev.get("type") or ev.get("EventType") or "event")\n            sub = ev.get("Subtype") or ev.get("EventSubType") or ""\n            desc = str(ev.get("Description") or "")[:160]\n            cat, w = classify_event(f"{etype} {sub} {desc}")\n            if w <= 0:\n                continue\n            events.append({\n                "id": ev.get("id") or ev.get("ID"),\n                "lat": float(lat), "lon": float(lon),\n                "type": f"{etype} {sub}".strip(),\n                "desc": desc, "category": cat, "weight": w,\n                "severity": ev.get("Severity"),\n                "full_closure": bool(ev.get("IsFullClosure")),\n            })\n        return events\n    return None\n\n\n# TomTom iconCategory -> (category, weight). Weather-condition warnings\n# (fog / rain / wind) are weighted 0.0 because the weather multiplier\n# already models conditions — counting them again would double-dip.\nTOMTOM_ICON_CATEGORIES = {\n    1: ("crash", 1.0),\n    3: ("closure/hazard", 0.6), 5: ("closure/hazard", 0.6),\n    8: ("closure/hazard", 0.6), 11: ("closure/hazard", 0.6),\n    14: ("stalled/disabled", 0.4),\n    6: ("congestion", 0.3),\n    7: ("roadwork", 0.15), 9: ("roadwork", 0.15),\n    2: ("weather-warning", 0.0), 4: ("weather-warning", 0.0),\n    10: ("weather-warning", 0.0),\n}\n\n_TOMTOM_FIELDS = ("{incidents{type,geometry{type,coordinates},properties{"\n                  "id,iconCategory,magnitudeOfDelay,roadNumbers,"\n                  "events{description,iconCategory}}}}")\n\n\ndef _tomtom_point(geom):\n    """First (lat, lon) from a TomTom incident geometry (Point or\n    LineString; coordinates are [lon, lat])."""\n    c = (geom or {}).get("coordinates")\n    if not c:\n        return None\n    t = (geom or {}).get("type")\n    if t == "Point":\n        return (float(c[1]), float(c[0]))\n    if t in ("LineString", "MultiPoint"):\n        return (float(c[0][1]), float(c[0][0]))\n    if t == "MultiLineString":\n        return (float(c[0][0][1]), float(c[0][0][0]))\n    return None\n\n\ndef _fetch_incidents_tomtom() -> list[dict]:\n    """Statewide incidents from the TomTom Traffic Incident Details API\n    (v5). The API caps each request\'s bbox at ~10,000 km^2, so the state\n    bbox is tiled into ~1-degree squares (about 20 requests per run —\n    well inside the free tier at a 20-minute cadence). Returns [] when\n    TOMTOM_API_KEY is not set."""\n    key = os.environ.get("TOMTOM_API_KEY", "").strip()\n    if not key:\n        print("[info] TOMTOM_API_KEY not set; skipping TomTom feed")\n        return []\n    b = config.STATE_BBOX\n    step = config.TOMTOM_TILE_DEG\n    events, seen, budget, tile_fails = [], set(), config.TOMTOM_MAX_REQUESTS, 0\n    lat = b["south"]\n    while lat < b["north"]:\n        lon = b["west"]\n        while lon < b["east"]:\n            if budget <= 0:\n                break\n            budget -= 1\n            n = min(lat + step, b["north"])\n            e = min(lon + step, b["east"])\n            params = {\n                "key": key,\n                "bbox": f"{lon},{lat},{e},{n}",   # minLon,minLat,maxLon,maxLat\n                "fields": _TOMTOM_FIELDS,\n                "language": "en-US",\n                "timeValidityFilter": "present",\n            }\n            try:\n                r = requests.get(config.TOMTOM_URL, params=params, timeout=20)\n                r.raise_for_status()\n                incidents = r.json().get("incidents") or []\n            except Exception as ex:  # noqa: BLE001\n                tile_fails += 1\n                print(f"[warn] tomtom tile ({lat:.1f},{lon:.1f}) failed: {ex}",\n                      file=sys.stderr)\n                lon += step\n                continue\n            for inc in incidents:\n                p = inc.get("properties") or {}\n                iid = p.get("id")\n                if iid and iid in seen:\n                    continue  # incidents can straddle tile edges\n                if iid:\n                    seen.add(iid)\n                pt = _tomtom_point(inc.get("geometry"))\n                if pt is None:\n                    continue\n                descs = [str(ev.get("description") or "")\n                         for ev in (p.get("events") or [])]\n                desc = "; ".join(d for d in descs if d)\n                icon = p.get("iconCategory")\n                if icon in TOMTOM_ICON_CATEGORIES:\n                    cat, w = TOMTOM_ICON_CATEGORIES[icon]\n                else:  # unknown icon -> fall back to text classification\n                    cat, w = classify_event(desc)\n                if w <= 0:\n                    continue  # weather warnings etc.\n                roads = "/".join(p.get("roadNumbers") or [])\n                title = (desc.split(";")[0].strip().capitalize() or cat)\n                if roads:\n                    title += f" on {roads}"\n                events.append({\n                    "id": iid,\n                    "lat": pt[0], "lon": pt[1],\n                    "type": title[:80],\n                    "desc": (desc or title)[:160],\n                    "category": cat, "weight": w,\n                    "severity": p.get("magnitudeOfDelay"),\n                    "full_closure": icon == 8,\n                    "src": "tomtom",\n                })\n            lon += step\n        lat += step\n    if tile_fails and not events:\n        raise RuntimeError(f"all TomTom tiles failed ({tile_fails})")\n    return events\n\n\ndef _norm_name(s: str) -> str:\n    import re\n    return re.sub(r"[^a-z]", "", str(s).lower())\n\n\ndef _point_in_ring(lat, lon, ring) -> bool:\n    """Ray-cast point-in-polygon. ring = [[lon, lat], ...]."""\n    inside = False\n    j = len(ring) - 1\n    for i in range(len(ring)):\n        xi, yi = ring[i][0], ring[i][1]\n        xj, yj = ring[j][0], ring[j][1]\n        if ((yi > lat) != (yj > lat)) and (\n                lon < (xj - xi) * (lat - yi) / (yj - yi) + xi):\n            inside = not inside\n        j = i\n    return inside\n\n\ndef _point_in_geom(lat, lon, geom) -> bool:\n    polys = ([geom["coordinates"]] if geom["type"] == "Polygon"\n             else geom["coordinates"])\n    for poly in polys:  # poly[0] = outer ring, rest = holes\n        if _point_in_ring(lat, lon, poly[0]):\n            if any(_point_in_ring(lat, lon, hole) for hole in poly[1:]):\n                continue\n            return True\n    return False\n\n\ndef _county_polygon_index(geodata, county_names):\n    """[(name, bbox, geometry)] for exact event->county assignment.\n    Names are canonicalized to the opportunity table\'s spelling (e.g.\n    Census "LaPorte" -> CSV "Laporte") so risk boosts join correctly."""\n    if not geodata:\n        return None\n    canon = {_norm_name(n): n for n in (county_names or [])}\n    index = []\n    for f in geodata.get("features", []):\n        name = f.get("properties", {}).get("NAME") or f.get("id")\n        name = canon.get(_norm_name(name), name)\n        geom = f.get("geometry") or {}\n        pts = []\n        polys = ([geom["coordinates"]] if geom.get("type") == "Polygon"\n                 else geom.get("coordinates") or [])\n        for poly in polys:\n            pts.extend(poly[0])\n        if not pts:\n            continue\n        lons = [p[0] for p in pts]\n        lats = [p[1] for p in pts]\n        index.append((name, (min(lats), max(lats), min(lons), max(lons)),\n                      geom))\n    return index or None\n\n\ndef _assign_county(lat, lon, poly_index) -> str | None:\n    for name, (s, n, w, e), geom in poly_index:\n        if s <= lat <= n and w <= lon <= e and _point_in_geom(lat, lon,\n                                                              geom):\n            return name\n    return None\n\n\ndef fetch_incidents(geodata=None,\n                    county_names=None) -> tuple[pd.Series, list[dict]]:\n    """(county -> weighted activity, event-level list). Events are\n    assigned to the county whose polygon contains them (all 92 counties;\n    out-of-state events from the bbox overhang into Chicago/Louisville/\n    Cincinnati are dropped exactly). If county boundaries are unavailable,\n    falls back to the old nearest-dynamic-centroid-within-35km rule.\n    Primary sources: 511in.org GraphQL (INDOT-managed roads; legacy JSON\n    endpoints as fallback) blended with TomTom probe-data incidents (adds\n    local roads). TomTom events within TOMTOM_DEDUP_KM of a 511 event are\n    dropped so nothing is double-counted."""\n    poly_index = _county_polygon_index(geodata, county_names)\n    counts = ({n: 0 for n in county_names} if poly_index and county_names\n              else {n: 0 for n in config.COUNTY_CENTROIDS})\n    raw = None\n    try:\n        raw = _fetch_incidents_graphql()\n        print(f"[info] 511 incident feed OK (graphql): "\n              f"{len(raw)} event(s) statewide")\n    except Exception as e:  # noqa: BLE001\n        print(f"[warn] 511 graphql feed failed: {e}", file=sys.stderr)\n    if raw is None:\n        raw = _fetch_incidents_legacy()\n    if raw is None:\n        print("[warn] incident feed unavailable: all event feeds failed",\n              file=sys.stderr)\n        raw = []\n    # --- TomTom blend (local-road coverage) --------------------------------\n    try:\n        tomtom = _fetch_incidents_tomtom()\n    except Exception as e:  # noqa: BLE001\n        print(f"[warn] tomtom incident feed failed: {e}", file=sys.stderr)\n        tomtom = []\n    if tomtom:\n        deg = config.TOMTOM_DEDUP_KM / 111.0\n        kept = [tv for tv in tomtom\n                if not any(abs(tv["lat"] - ev["lat"]) < deg\n                           and abs(tv["lon"] - ev["lon"]) < deg\n                           for ev in raw)]\n        print(f"[info] tomtom incident feed OK: {len(tomtom)} event(s) "\n              f"statewide, {len(kept)} kept after 511 de-dup")\n        raw = raw + kept\n    events: list[dict] = []\n    for ev in raw:\n        if poly_index:  # exact assignment, all 92 counties\n            best = _assign_county(ev["lat"], ev["lon"], poly_index)\n        else:  # fallback: nearest dynamic centroid within ~35 km\n            best, bd = None, 0.35\n            for name, (cla, clo) in config.COUNTY_CENTROIDS.items():\n                d = math.hypot(ev["lat"] - cla, ev["lon"] - clo)\n                if d < bd:\n                    best, bd = name, d\n        if best:\n            counts.setdefault(best, 0)\n            counts[best] += ev.get("weight", 0.3)  # severity-weighted\n            events.append({**ev, "county": best})\n    if raw:\n        cats: dict[str, int] = {}\n        for ev in events:\n            c = ev.get("category", "other")\n            cats[c] = cats.get(c, 0) + 1\n        breakdown = ", ".join(f"{v} {k}" for k, v in\n                              sorted(cats.items(), key=lambda x: -x[1]))\n        scope = ("Indiana counties (statewide polygons)" if poly_index\n                 else "dynamic counties (centroid fallback)")\n        print(f"[info] {len(events)} of {len(raw)} event(s) mapped to "\n              f"{scope} ({breakdown or \'none\'})")\n    return pd.Series(counts), events\n'
_FILES['memory.py'] = '"""\nTrailing-incident memory.\n\nEvery run logs the live 511 events it observes. Events that have dropped\noff the live feed become "trailing": their importance decays as\n\n    importance = magnitude * 0.5 ** (hours_since_last_seen / HALF_LIFE_H)\n\nso a magnitude-5 pileup from 3 days ago still registers (~0.35 of its\noriginal weight at the default 48 h half-life) while minor events fade\nfast. Trailing weight boosts county risk at half the per-point strength\nof a live event and is drawn on the map as fading markers.\n\nPersistence: JSON file. In Colab, mount Google Drive to keep memory\nacross sessions; otherwise memory covers the current session only.\n"""\nfrom __future__ import annotations\n\nimport datetime as dt\nimport hashlib\nimport json\nimport os\n\nimport config\n\nMAGNITUDE_KEYWORDS = [\n    ("pileup", 5), ("multi vehicle", 5), ("multi-vehicle", 5),\n    ("closed", 4), ("closure", 4), ("hazmat", 4), ("spill", 4),\n    ("overturned", 4), ("jackknif", 4),\n    ("crash", 3), ("accident", 3), ("collision", 3),\n    ("lane blocked", 2), ("lane closed", 2), ("restriction", 2),\n    ("stalled", 1), ("disabled", 1), ("construction", 1),\n]\nDEFAULT_MAGNITUDE = 2\n\n\ndef magnitude_of(event_type: str, desc: str = "", severity=None,\n                 full_closure: bool = False) -> int:\n    t = f"{event_type} {desc}".lower()\n    mag = DEFAULT_MAGNITUDE\n    for kw, m in MAGNITUDE_KEYWORDS:\n        if kw in t:\n            mag = m\n            break\n    if full_closure:\n        mag = max(mag, 4)\n    try:  # Castle Rock Severity is often "1".."5"\n        mag = max(mag, min(int(severity), 5))\n    except (TypeError, ValueError):\n        pass\n    return mag\n\n\ndef event_id(ev: dict) -> str:\n    raw = ev.get("id") or f\'{ev.get("lat"):.3f}|{ev.get("lon"):.3f}|\' \\\n                          f\'{ev.get("type", "")}\'\n    return hashlib.md5(str(raw).encode()).hexdigest()[:12]\n\n\ndef history_path() -> str:\n    envp = os.environ.get("HISTORY_PATH")\n    if envp:\n        return envp\n    drive = "/content/drive/MyDrive/accident_forecaster"\n    if os.path.isdir("/content/drive/MyDrive"):\n        os.makedirs(drive, exist_ok=True)\n        return f"{drive}/incident_history.json"\n    return "incident_history.json"\n\n\ndef load() -> dict:\n    """Local history, merged with the machine-built archive from the\n    GitHub Actions poller when config.INCIDENT_LOG_URL is set. The remote\n    archive wins for events it knows about (its last_seen is fresher)."""\n    hist = {}\n    try:\n        with open(history_path()) as f:\n            hist = json.load(f)\n    except Exception:  # noqa: BLE001\n        pass\n    if config.INCIDENT_LOG_URL:\n        try:\n            import requests\n            remote = requests.get(config.INCIDENT_LOG_URL, timeout=30).json()\n            for eid, e in remote.items():\n                if (eid not in hist\n                        or e["last_seen"] >= hist[eid]["last_seen"]):\n                    hist[eid] = e\n            print(f"[info] merged {len(remote)} events from remote archive")\n        except Exception as e:  # noqa: BLE001\n            print(f"[warn] remote incident archive unavailable: {e}")\n    return hist\n\n\ndef save(hist: dict) -> None:\n    with open(history_path(), "w") as f:\n        json.dump(hist, f)\n\n\ndef update(hist: dict, live_events: list[dict],\n           now: dt.datetime | None = None) -> dict:\n    """Merge this run\'s live events into history; prune fully-faded ones."""\n    now = now or dt.datetime.now()\n    now_s = now.isoformat(timespec="seconds")\n    live_ids = set()\n    for ev in live_events:\n        if ev.get("weight", 1.0) < config.MEMORY_MIN_WEIGHT:\n            continue  # roadwork/congestion: no trailing value, bloats file\n        eid = event_id(ev)\n        live_ids.add(eid)\n        if eid in hist:\n            hist[eid]["last_seen"] = now_s\n        else:\n            hist[eid] = {\n                "first_seen": now_s, "last_seen": now_s,\n                "lat": ev.get("lat"), "lon": ev.get("lon"),\n                "county": ev.get("county"), "type": ev.get("type", "event"),\n                "desc": ev.get("desc", ""),\n                "magnitude": magnitude_of(ev.get("type", ""),\n                                          ev.get("desc", ""),\n                                          ev.get("severity"),\n                                          ev.get("full_closure", False)),\n            }\n    # prune events decayed below 5% of a magnitude-1 event\n    keep = {}\n    for eid, e in hist.items():\n        if eid in live_ids or _decay(e, now) * e["magnitude"] >= 0.05:\n            keep[eid] = e\n    return keep\n\n\ndef _decay(e: dict, now: dt.datetime) -> float:\n    last = dt.datetime.fromisoformat(e["last_seen"])\n    hours = max((now - last).total_seconds() / 3600, 0)\n    return 0.5 ** (hours / config.TRAILING_HALF_LIFE_H)\n\n\ndef trailing(hist: dict, live_ids: set[str],\n             now: dt.datetime | None = None) -> list[dict]:\n    """Decayed non-live events: [{lat, lon, county, type, magnitude,\n    importance, age_h}] with importance = magnitude * decay."""\n    now = now or dt.datetime.now()\n    out = []\n    for eid, e in hist.items():\n        if eid in live_ids:\n            continue\n        d = _decay(e, now)\n        imp = e["magnitude"] * d\n        if imp < 0.05:\n            continue\n        age_h = (now - dt.datetime.fromisoformat(e["last_seen"])\n                 ).total_seconds() / 3600\n        out.append({**e, "importance": round(imp, 3),\n                    "decay": round(d, 3), "age_h": round(age_h, 1)})\n    return sorted(out, key=lambda x: -x["importance"])\n\n\ndef county_trailing_scores(trail: list[dict]) -> dict[str, float]:\n    scores: dict[str, float] = {}\n    for e in trail:\n        c = e.get("county")\n        if c:\n            scores[c] = scores.get(c, 0.0) + e["importance"]\n    return scores\n'
_FILES['model.py'] = '"""\nCounty risk model (v2).\n\nrisk(county, hour) = opportunity(county) × exposure(hour-of-week)\n                     × weather_multiplier(county, hour)\ncounty_score       = Σ_hours risk × incident_boost(county)\n\nStatic-tier counties (outside the top DYNAMIC_N) use neutral weather, so\ntheir score is pure baseline — they can\'t gain from conditions but remain\nin the allocation.\n"""\nfrom __future__ import annotations\n\nimport numpy as np\nimport pandas as pd\n\nimport config\nfrom sources import weather_multiplier\n\n\ndef hourly_exposure() -> np.ndarray:\n    vec = []\n    for wd in range(7):\n        prof = config.WEEKDAY_HOURLY if wd < 5 else config.WEEKEND_HOURLY\n        vec.extend(prof)\n    v = np.array(vec, dtype=float)\n    return v / v.mean()\n\n\ndef score(counties: pd.DataFrame,\n          weather: dict[str, pd.DataFrame],\n          incidents: pd.Series,\n          hours: int = 48,\n          trailing_scores: dict[str, float] | None = None\n          ) -> tuple[pd.DataFrame, pd.DataFrame]:\n    """Returns (county_scores, county_hourly)."""\n    exposure = hourly_exposure()\n    hourly_rows = []\n    horizon, drivers = {}, {}\n    for _, c in counties.iterrows():\n        name = c["county"]\n        wdf = weather.get(name)\n        if c["dynamic"] and wdf is not None:\n            total = 0.0\n            cond_hours: dict[str, int] = {}\n            peak_ts, peak_risk = None, -1.0\n            for ts, row in wdf.iloc[:hours].iterrows():\n                wm = weather_multiplier(row)\n                ex = float(exposure[ts.weekday() * 24 + ts.hour])\n                risk = wm * ex\n                total += risk\n                lab = condition_label(row)\n                if lab:\n                    cond_hours[lab] = cond_hours.get(lab, 0) + 1\n                if risk > peak_risk:\n                    peak_risk, peak_ts = risk, ts\n                hourly_rows.append({\n                    "county": name, "time": ts,\n                    "weather_mult": round(wm, 3), "exposure": round(ex, 3),\n                    "hour_risk": round(risk, 3),\n                })\n            horizon[name] = total\n            cond_txt = ", ".join(f"{v}h {k}" for k, v in sorted(\n                cond_hours.items(), key=lambda x: -x[1])) or "clear/dry"\n            drivers[name] = {\n                "conditions": cond_txt,\n                "peak": peak_ts.strftime("%a %H:00") if peak_ts is not None\n                        else "",\n            }\n        else:\n            horizon[name] = float(hours)  # neutral: mean multiplier 1.0\n\n    out = counties.copy()\n    out["horizon_weather"] = out["county"].map(horizon)\n    out["wx_conditions"] = out["county"].map(\n        lambda n: drivers.get(n, {}).get("conditions", "static tier"))\n    out["wx_peak"] = out["county"].map(\n        lambda n: drivers.get(n, {}).get("peak", ""))\n    out["weather_lift"] = (out["horizon_weather"] / hours).round(3)\n    ev = out["county"].map(incidents).fillna(0)\n    out["active_events"] = ev.round(2)  # severity-weighted activity\n    tr = out["county"].map(trailing_scores or {}).fillna(0.0)\n    out["trailing_weight"] = tr.round(2)\n    out["incident_boost"] = np.minimum(\n        1 + config.INCIDENT_BOOST_PER_EVENT * ev\n        + config.TRAILING_BOOST_PER_POINT * tr,\n        config.INCIDENT_BOOST_CAP)\n    out["risk_score"] = (out["opportunity_score"] * out["horizon_weather"]\n                         * out["incident_boost"])\n    out["risk_score"] = (100 * out["risk_score"]\n                         / out["risk_score"].max()).round(2)\n    out = out.sort_values("risk_score", ascending=False)\n    return out, pd.DataFrame(hourly_rows)\n\n\ndef condition_label(row) -> str | None:\n    """Human label for the dominant adverse condition in a forecast hour."""\n    rain = row.get("precipitation", 0.0)\n    snow = row.get("snowfall", 0.0)\n    if row.get("freezing_precip_flag", False) or (\n            rain > 0.1 and row.get("temperature_2m", 15) <= 0):\n        return "freezing rain/ice"\n    if snow > 1.0:\n        return "heavy snow"\n    if snow > 0.1:\n        return "snow"\n    if rain > 2.5:\n        return "heavy rain"\n    if rain > 0.1:\n        return "rain"\n    if row.get("visibility", 20000) < 1000:\n        return "fog"\n    return None\n'
_FILES['geo.py'] = '"""\nGeometry + commuter-flow data (fetched live in Colab; graceful fallbacks).\n\n- Indiana county polygons: Census/plotly counties GeoJSON, filtered to\n  STATE == "18". Gives real boundaries for the choropleth and centroids\n  computed from the polygons (replacing hand-entered centroids for flows).\n- Commuter origins: Census LODES8 origin-destination files for Indiana\n  (block-level home->work job counts, aggregated to county). The inbound\n  commuter mix of a county is the best public proxy for "where the people\n  driving through it come from." main = both ends in Indiana; aux adds\n  out-of-state residents working in Indiana (rolled up to state level).\n"""\nfrom __future__ import annotations\n\nimport re\nimport sys\n\nimport pandas as pd\nimport requests\n\nGEOJSON_URL = ("https://raw.githubusercontent.com/plotly/datasets/master/"\n               "geojson-counties-fips.json")\nLODES_MAIN = ("https://lehd.ces.census.gov/data/lodes/LODES8/in/od/"\n              "in_od_main_JT00_2022.csv.gz")\nLODES_AUX = ("https://lehd.ces.census.gov/data/lodes/LODES8/in/od/"\n             "in_od_aux_JT00_2022.csv.gz")\n\nNEIGHBOR_STATES = {  # state FIPS -> (label, approx centroid) for flow lines\n    "17": ("Illinois", (40.0, -89.2)), "21": ("Kentucky", (37.5, -85.3)),\n    "39": ("Ohio", (40.3, -82.8)), "26": ("Michigan", (43.3, -84.6)),\n}\nSTATE_NAMES = {"17": "Illinois", "21": "Kentucky", "39": "Ohio",\n               "26": "Michigan", "state": "out of state"}\n\n\ndef _norm(s: str) -> str:\n    return re.sub(r"[^a-z]", "", str(s).lower())\n\n\ndef fetch_indiana_geojson() -> dict | None:\n    """{\'features\': [...], \'fips_to_name\': {}, \'name_to_fips\': {},\n        \'centroids\': {fips: (lat, lon)}} or None if unreachable."""\n    try:\n        gj = requests.get(GEOJSON_URL, timeout=90).json()\n    except Exception as e:  # noqa: BLE001\n        print(f"[warn] county GeoJSON unavailable ({e}); map falls back "\n              "to geographic circles.", file=sys.stderr)\n        return None\n    feats = [f for f in gj["features"]\n             if f.get("properties", {}).get("STATE") == "18"]\n    fips_to_name, centroids = {}, {}\n    for f in feats:\n        fips = f.get("id") or ("18" + f["properties"]["COUNTY"])\n        f["id"] = fips\n        name = f["properties"].get("NAME", fips)\n        fips_to_name[fips] = name\n        centroids[fips] = _centroid(f["geometry"])\n    return {"features": feats, "fips_to_name": fips_to_name,\n            "name_to_fips": {_norm(v): k for k, v in fips_to_name.items()},\n            "centroids": centroids}\n\n\ndef _centroid(geom: dict) -> tuple[float, float]:\n    if geom["type"] == "Polygon":\n        ring = geom["coordinates"][0]\n    else:  # MultiPolygon: use largest ring\n        ring = max((p[0] for p in geom["coordinates"]), key=len)\n    lon = sum(p[0] for p in ring) / len(ring)\n    lat = sum(p[1] for p in ring) / len(ring)\n    return (round(lat, 4), round(lon, 4))\n\n\ndef fetch_flows() -> pd.DataFrame | None:\n    """County-level OD: columns w_cty (workplace FIPS, all 18xxx),\n    h_cty (home FIPS, any state), jobs. None if unreachable."""\n    frames = []\n    for url in (LODES_MAIN, LODES_AUX):\n        try:\n            df = pd.read_csv(url, usecols=["w_geocode", "h_geocode", "S000"],\n                             dtype={"w_geocode": str, "h_geocode": str})\n            frames.append(df)\n        except Exception as e:  # noqa: BLE001\n            print(f"[warn] LODES fetch failed for {url.split(\'/\')[-1]} "\n                  f"({e})", file=sys.stderr)\n    if not frames:\n        return None\n    od = pd.concat(frames, ignore_index=True)\n    od["w_cty"] = od["w_geocode"].str[:5]\n    od["h_cty"] = od["h_geocode"].str[:5]\n    return (od.groupby(["w_cty", "h_cty"])["S000"].sum()\n            .rename("jobs").reset_index())\n\n\ndef top_origins(flows: pd.DataFrame | None, geo: dict | None,\n                k: int = 6) -> dict:\n    """{workplace_fips: [{label, share, lat, lon(or None)}...]} — the top\n    inbound commuter origins for every Indiana county."""\n    if flows is None:\n        return {}\n    fips_to_name = geo["fips_to_name"] if geo else {}\n    centroids = geo["centroids"] if geo else {}\n    out = {}\n    for w, grp in flows.groupby("w_cty"):\n        total = grp["jobs"].sum()\n        ext = grp[grp["h_cty"] != w].copy()\n        # roll out-of-state homes up to state level\n        ext["origin"] = ext["h_cty"].where(\n            ext["h_cty"].str.startswith("18"), "S" + ext["h_cty"].str[:2])\n        agg = (ext.groupby("origin")["jobs"].sum()\n               .sort_values(ascending=False).head(k))\n        rows = []\n        for origin, jobs in agg.items():\n            if origin.startswith("S"):\n                sf = origin[1:]\n                label, cen = NEIGHBOR_STATES.get(\n                    sf, (STATE_NAMES.get(sf, f"state {sf}"), None))\n            else:\n                label = fips_to_name.get(origin, origin) + " Co."\n                cen = centroids.get(origin)\n            rows.append({\n                "label": label, "share": round(float(jobs / total), 3),\n                "lat": cen[0] if cen else None,\n                "lon": cen[1] if cen else None,\n            })\n        internal = float(grp[grp["h_cty"] == w]["jobs"].sum() / total)\n        out[w] = {"origins": rows, "internal_share": round(internal, 3)}\n    return out\n'
_FILES['mapbuild.py'] = '"""\nInteractive risk map (v4).\n\nCollisions vs contributing factors: only actual crashes get map markers.\nEverything else — weather, recent-incident echo, closures/roadwork/\ncongestion — shapes the county COLOR, and the county click-panel itemizes\nexactly what fed it. Alongside the HTML, build_map writes\nlive_markers.json; the open page re-fetches it (same-origin, no keys)\nevery 5 minutes so markers and colors track the newest pipeline run\nwithout a manual reload.\n\n- County polygons (choropleth) shaded by risk — geometry is geographic, so\n  it stays county-sized at every zoom level. Fallback when the GeoJSON\n  can\'t be fetched: L.circle with a radius in *meters* (also zoom-correct).\n- Hover tooltip explains WHY a county is hot: EMS opportunity rank and\n  injured patients/yr, weather conditions in the horizon and the peak-risk\n  window, active incidents, and the county\'s top inbound commuter origins.\n- Click a county to draw its commuter inflow lines (Census LODES): where\n  the people traveling through it come from, line weight ~ share.\n- LIVE MODE: while the page is open it re-fetches Open-Meteo every 15\n  minutes for the dynamic counties, recomputes the weather multipliers in\n  the browser, and recolors the map. Incidents refresh on notebook re-run.\n"""\nfrom __future__ import annotations\n\nimport datetime as _dt\nimport json\nimport os\n\nimport config\n\n\ndef build_map(scores, geo, origins, path, hours=48, trail=None,\n              live=None):\n    fips_by_norm = geo["name_to_fips"] if geo else {}\n    import re\n    norm = lambda s: re.sub(r"[^a-z]", "", str(s).lower())  # noqa: E731\n\n    counties_js = {}\n    for r in scores.itertuples():\n        fips = fips_by_norm.get(norm(r.county))\n        cen = (geo["centroids"].get(fips) if geo and fips\n               else config.COUNTY_CENTROIDS.get(r.county))\n        counties_js[r.county] = {\n            "fips": fips, "risk": float(r.risk_score),\n            "rank": int(r.rank), "injured": float(r.injured_patients),\n            "serious": float(r.serious_cases),\n            "opportunity": float(r.opportunity_score),\n            "metro": r.metro, "dynamic": bool(r.dynamic),\n            "events": float(r.active_events),\n            "incBoost": float(r.incident_boost),\n            "lift": float(r.weather_lift),\n            "wx": r.wx_conditions, "peak": r.wx_peak,\n            "trailing": float(r.trailing_weight),\n            "lat": cen[0] if cen else None,\n            "lon": cen[1] if cen else None,\n            "flows": (origins.get(fips, {}) if fips else {}),\n        }\n\n    features = geo["features"] if geo else []\n    generated = _dt.datetime.now().isoformat(timespec="seconds")\n    payload = {\n        "counties": counties_js,\n        "generated": generated,\n        "minMarkerW": config.MAP_MIN_MARKER_WEIGHT,\n        "live": [\n            {"lat": e["lat"], "lon": e["lon"],\n             "type": str(e.get("type", "event"))[:80],\n             "desc": str(e.get("desc", ""))[:160],\n             "cat": e.get("category", "other"),\n             "w": e.get("weight", 0.3),\n             "county": e.get("county", "")}\n            for e in (live or []) if e.get("lat") is not None],\n        "features": features,\n        "hours": hours,\n        "exposure": _exposure_vec(),\n        "dynamicCentroids": {\n            c: {"lat": counties_js[c]["lat"], "lon": counties_js[c]["lon"]}\n            for c, v in counties_js.items()\n            if v["dynamic"] and v["lat"] is not None},\n    }\n\n    html = _TEMPLATE.replace("__PAYLOAD__", json.dumps(payload))\n    with open(path, "w") as f:\n        f.write(html)\n    # Sidecar consumed by the open page\'s 5-minute incident auto-refresh\n    # (same-origin fetch; must be published next to the HTML).\n    refresh = {\n        "generated": generated,\n        "live": payload["live"],\n        "counties": {n: {"events": c["events"], "incBoost": c["incBoost"],\n                         "trailing": c["trailing"]}\n                     for n, c in counties_js.items()},\n    }\n    side = os.path.join(os.path.dirname(path) or ".", "live_markers.json")\n    with open(side, "w") as f:\n        json.dump(refresh, f)\n\n\ndef _exposure_vec():\n    vec = []\n    for wd in range(7):\n        prof = config.WEEKDAY_HOURLY if wd < 5 else config.WEEKEND_HOURLY\n        vec.extend(prof)\n    mean = sum(vec) / len(vec)\n    return [round(v / mean, 4) for v in vec]\n\n\n_TEMPLATE = r"""<!DOCTYPE html><html><head><meta charset="utf-8">\n<meta name="viewport" content="width=device-width, initial-scale=1">\n<title>Indiana Accident Risk — Live</title>\n<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.min.css"/>\n<script src="https://cdnjs.cloudflare.com/ajax/libs/leaflet/1.9.4/leaflet.min.js"></script>\n<style>\nhtml,body{height:100%;margin:0;font-family:system-ui,sans-serif}\n#map{height:100%;transition:margin-right .25s}\nbody.panel-open #map{margin-right:360px}\n.panel-box{background:#fff;padding:8px 12px;border-radius:6px;font-size:13px;\nbox-shadow:0 1px 4px rgba(0,0,0,.3)}\n.leaflet-tooltip.hint{font-size:12px;font-weight:600;padding:3px 8px}\n.leaflet-interactive{cursor:pointer}\n#side{position:fixed;top:0;right:-380px;width:360px;height:100%;background:#fff;\nbox-shadow:-2px 0 10px rgba(0,0,0,.25);transition:right .25s;z-index:1200;\noverflow-y:auto;box-sizing:border-box;padding:16px 18px}\nbody.panel-open #side{right:0}\n#side h2{margin:0 44px 4px 0;font-size:18px}\n#side .badge{display:inline-block;padding:1px 9px;border-radius:10px;color:#fff;font-weight:700}\n#side .sec{margin:14px 0 0}\n#side .sec h3{margin:0 0 4px;font-size:12px;text-transform:uppercase;letter-spacing:.05em;color:#777}\n#side .sec div{font-size:13.5px;line-height:1.5}\n#closebtn{position:absolute;top:10px;right:12px;border:none;background:#eee;\nborder-radius:50%;width:28px;height:28px;font-size:15px;cursor:pointer}\n.origin-row{display:flex;justify-content:space-between;border-bottom:1px solid #f0f0f0;padding:2px 0}\n.bar{height:6px;background:#08519c;border-radius:3px;margin-top:2px}\n.inc{display:flex;align-items:center;justify-content:center;border-radius:50%;\nbackground:#fff;font-weight:700;box-shadow:0 1px 6px rgba(0,0,0,.6);\nline-height:1}\n.inc.live{border:3px solid #c62828;color:#c62828}\n.inc.past{border:3px solid #5e35b1;color:#5e35b1}\n@media (max-width:700px){#side{width:85%}body.panel-open #map{margin-right:0}}\n</style></head><body><div id="map"></div>\n<div id="side"><button id="closebtn" onclick="closePanel()">×</button><div id="sidecontent"></div></div>\n<script>\nconst D = __PAYLOAD__;\nconst map = L.map(\'map\').setView([39.9,-86.3],7);\nL.tileLayer(\'https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png\',\n {attribution:\'&copy; OpenStreetMap · risk: EMS opportunity × weather × exposure · flows: Census LODES\'}).addTo(map);\n\nconst byFips = {};\nObject.entries(D.counties).forEach(([name,c])=>{ if(c.fips) byFips[c.fips]={name,...c}; });\n\nfunction color(r){return r>60?\'#67000d\':r>40?\'#a50f15\':r>25?\'#cb181d\':r>15?\'#ef3b2c\':r>8?\'#fb6a4a\':r>4?\'#fcae91\':\'#fee5d9\';}\n\n// ---------- side panel ----------\nfunction sec(title, body){ return `<div class="sec"><h3>${title}</h3><div>${body}</div></div>`; }\nfunction panelHTML(c,name){\n  let h = `<h2>${name} County <span class="badge" style="background:${color(c.risk)}">risk ${c.risk.toFixed(1)}</span></h2>`;\n  h += `<div style="color:#777;font-size:12.5px">${c.metro===\'other_in\'?\'outside core metros\':c.metro+\' market\'} · next ${D.hours}h</div>`;\n  h += sec(\'Why this county scores here\',\n    `#${c.rank} statewide in EMS-measured crash injuries — ${Math.round(c.injured).toLocaleString()} injured patients/yr, ${Math.round(c.serious)} serious cases.`);\n  if(c.dynamic){\n    h += sec(\'Weather (next \'+D.hours+\'h)\',\n      `${c.wx}. Overall risk lift <b>×${c.lift.toFixed(2)}</b>${c.peak?`; peak-risk window <b>${c.peak}</b>`:\'\'}.`);\n  } else {\n    h += sec(\'Weather\', \'<i>Static tier — baseline only, no live weather feed for this county.</i>\');\n  }\n  let inc=\'\';\n  const f=countyFactors(name), crashes=f[\'crash\']||0;\n  if(crashes) inc += `<b>⚠ ${crashes} active crash${crashes>1?\'es\':\'\'}</b> — marked on the map.<br>`;\n  const parts=Object.entries(f).filter(([k])=>k!==\'crash\')\n    .map(([k,v])=>`${v} ${FACTOR_LABEL[k]||k}`);\n  if(parts.length) inc += `<b>Traffic factors</b> shading this county (no markers): ${parts.join(\', \')}.<br>`;\n  if(c.trailing>0.05) inc += `<b>Recent-incident echo</b>: weight <b>${c.trailing.toFixed(1)}</b> from significant events in the last ~2 days, fading as they age.<br>`;\n  if(inc) inc += `Together these boost this county\'s color ×${c.incBoost.toFixed(2)} on top of its injury baseline and weather.`;\n  h += sec(\'Live & recent traffic (feeds the color)\', inc ||\n    \'Nothing live or recent — color here is injury baseline × weather only.\');\n  if(c.flows && c.flows.origins && c.flows.origins.length){\n    let rows = `<div class="origin-row"><span>local (lives in-county)</span><b>${((c.flows.internal_share||0)*100).toFixed(0)}%</b></div>`;\n    const mx = Math.max(...c.flows.origins.map(o=>o.share));\n    c.flows.origins.forEach(o=>{\n      rows += `<div class="origin-row"><span>${o.label}</span><b>${(o.share*100).toFixed(1)}%</b></div>`+\n              `<div class="bar" style="width:${(o.share/mx*100).toFixed(0)}%"></div>`;\n    });\n    rows += `<div style="color:#777;font-size:12px;margin-top:6px">Census commuter data — blue lines on the map show these inflows. Counties above are candidates to include in this hotspot\'s ad geography.</div>`;\n    h += sec(\'Who travels through (origins)\', rows);\n  }\n  return h;\n}\nconst FACTOR_LABEL={\'closure/hazard\':\'closures/hazards\',\'stalled/disabled\':\'stalled vehicles\',\n \'congestion\':\'congestion reports\',\'roadwork\':\'roadwork zones\',\'other\':\'other events\'};\nfunction countyFactors(name){\n  const f={};(D.live||[]).forEach(t=>{if(t.county===name)f[t.cat]=(f[t.cat]||0)+1;});\n  return f;\n}\nfunction openPanel(html){ document.getElementById(\'sidecontent\').innerHTML=html; document.body.classList.add(\'panel-open\'); setTimeout(()=>map.invalidateSize(),260); }\nfunction closePanel(){ document.body.classList.remove(\'panel-open\'); flowLayer.clearLayers(); setTimeout(()=>map.invalidateSize(),260); }\n\n// ---------- flows ----------\nlet flowLayer = L.layerGroup().addTo(map);\nfunction drawFlows(c){\n  flowLayer.clearLayers();\n  if(!(c.flows&&c.flows.origins&&c.lat)) return;\n  c.flows.origins.forEach(o=>{\n    if(o.lat==null) return;\n    L.polyline([[o.lat,o.lon],[c.lat,c.lon]],{color:\'#08519c\',weight:1+o.share*30,opacity:.65}).addTo(flowLayer);\n    L.circleMarker([o.lat,o.lon],{radius:4,color:\'#08519c\',fillOpacity:.9}).addTo(flowLayer);\n  });\n}\n\n// ---------- county layer ----------\nlet geoLayer=null, fallbackLayer=null;\nfunction styleFor(c){return {color:\'#555\',weight:c.events>0?2.5:1,\n  dashArray:c.events>0?\'4\':null,fillColor:color(c.risk),fillOpacity:.62};}\nconst hoverStyle={weight:3,color:\'#111\',fillOpacity:.8};\n\nfunction wireCounty(layer,c,name){\n  layer.bindTooltip(`${name} County — click for details`,{sticky:true,className:\'hint\',direction:\'top\'});\n  layer.on(\'mouseover\',()=>layer.setStyle(hoverStyle));\n  layer.on(\'mouseout\',()=>layer.setStyle(styleFor(c)));\n  layer.on(\'click\',()=>{openPanel(panelHTML(c,name)); drawFlows(c);});\n}\nfunction render(){\n  if(D.features && D.features.length){\n    if(geoLayer) map.removeLayer(geoLayer);\n    geoLayer = L.geoJSON({type:\'FeatureCollection\',features:D.features},{\n      style: f=>{const c=byFips[f.id]; return c?styleFor(c):{fillColor:\'#eee\',color:\'#999\',weight:.5,fillOpacity:.3};},\n      onEachFeature:(f,layer)=>{ const c=byFips[f.id]; if(c) wireCounty(layer,c,c.name); }}).addTo(map);\n  } else {\n    if(fallbackLayer) map.removeLayer(fallbackLayer);\n    fallbackLayer=L.layerGroup().addTo(map);\n    Object.entries(D.counties).forEach(([name,c])=>{\n      if(c.lat==null) return;\n      const layer=L.circle([c.lat,c.lon],{radius:13000,...styleFor(c)}).addTo(fallbackLayer);\n      wireCounty(layer,c,name);\n    });\n  }\n}\nrender();\n\n// ---------- incident markers (live + fading past) ----------\nfunction incIcon(cls, sym, size, alpha){\n  return L.divIcon({className:\'\', iconSize:[size,size], iconAnchor:[size/2,size/2],\n    html:`<div class="inc ${cls}" style="width:${size}px;height:${size}px;opacity:${alpha};font-size:${Math.round(size*0.62)}px">${sym}</div>`});\n}\nfunction agoTxt(h){ return h<48 ? Math.round(h)+\'h ago\' : (h/24).toFixed(1)+\'d ago\'; }\nfunction livePanelHTML(t){\n  let h = `<h2>⚠ Live incident <span class="badge" style="background:#c62828">active now</span></h2>`;\n  h += sec(\'What\', `${t.type}${t.desc?`<br><span style="color:#555">${t.desc}</span>`:\'\'}`);\n  h += sec(\'Where\', `${t.county} County — currently on the live incident feeds.`);\n  h += sec(\'Risk contribution\', `Classified as <b>${t.cat}</b> — weight ${t.w.toFixed(2)} toward this county\'s color.`);\n  return h;\n}\nconst liveLayer = L.layerGroup().addTo(map);\nfunction drawLive(){\n  liveLayer.clearLayers();\n  (D.live||[]).forEach(t=>{\n    if(t.w < D.minMarkerW) return; // factors shade the county, no marker\n    const m=L.marker([t.lat,t.lon],{icon:incIcon(\'live\',\'⚠\',30,1),zIndexOffset:1000+Math.round(t.w*100)}).addTo(liveLayer);\n    m.bindTooltip(`LIVE ${t.cat}: ${t.type}`,{sticky:true,className:\'hint\',direction:\'top\'});\n    m.on(\'click\',()=>openPanel(livePanelHTML(t)));\n  });\n}\ndrawLive();\n\n// ---------- legend + live status ----------\nconst legend=L.control({position:\'bottomright\'});\nlegend.onAdd=()=>{const d=L.DomUtil.create(\'div\',\'panel-box\');\n d.innerHTML=\'<b>County risk (next \'+D.hours+\'h)</b><br>\'+\n [[60,\'severe\'],[25,\'elevated\'],[8,\'moderate\'],[0,\'baseline\']].map(\n  ([v,t])=>`<span style="color:${color(v+0.1)}">■</span> ${t}`).join(\' &nbsp;\')+\n \'<br><span style="color:#c62828">⚠</span> live crash · color = injuries baseline × weather × traffic factors\'+\n \'<br>click any county or marker for details\'+\n \'<br><span id="livestatus" style="color:#666">live weather: starting…</span>\'+\n \'<br><span id="incstatus" style="color:#666">incidents: from this build</span>\';\n return d;};\nlegend.addTo(map);\n\n// ---------- LIVE MODE: refresh weather every 15 min ----------\nconst MULT={rl:1.25,rm:1.5,rh:1.8,sl:1.5,sm:1.8,sh:2.2,fz:2.5,fog:1.4,dark:1.25,wind:1.15};\nfunction wmult(p,s,t,vis,g,day,code){\n  let m=1;\n  if([56,57,66,67].includes(code)||(p>0.1&&t<=0)) m*=MULT.fz;\n  else if(s>2.5)m*=MULT.sh; else if(s>1)m*=MULT.sm; else if(s>0.1)m*=MULT.sl;\n  else if(p>7.6)m*=MULT.rh; else if(p>2.5)m*=MULT.rm; else if(p>0.1)m*=MULT.rl;\n  if(vis<1000)m*=MULT.fog; if(!day)m*=MULT.dark; if(g>40)m*=MULT.wind;\n  return Math.min(m,3.5);\n}\nasync function refreshWeather(){\n  const names=Object.keys(D.dynamicCentroids);\n  if(!names.length) return;\n  const lats=names.map(n=>D.dynamicCentroids[n].lat).join(\',\');\n  const lons=names.map(n=>D.dynamicCentroids[n].lon).join(\',\');\n  const url=`https://api.open-meteo.com/v1/forecast?latitude=${lats}&longitude=${lons}`+\n   `&hourly=precipitation,snowfall,temperature_2m,visibility,wind_gusts_10m,is_day,weather_code`+\n   `&forecast_days=${Math.min(7,Math.ceil(D.hours/24))}&timezone=America%2FIndiana%2FIndianapolis`;\n  try{\n    let payload=await (await fetch(url)).json();\n    if(!Array.isArray(payload)) payload=[payload];\n    names.forEach((name,i)=>{\n      const h=payload[i].hourly; let total=0;\n      for(let k=0;k<Math.min(D.hours,h.time.length);k++){\n        const ts=new Date(h.time[k]);\n        const ex=D.exposure[((ts.getDay()+6)%7)*24+ts.getHours()];\n        total+=wmult(h.precipitation[k],h.snowfall[k],h.temperature_2m[k],\n                     h.visibility[k],h.wind_gusts_10m[k],h.is_day[k],h.weather_code[k])*ex;\n      }\n      const c=D.counties[name];\n      c.lift=total/D.hours;\n    });\n    recolor();\n    document.getElementById(\'livestatus\').textContent=\'live weather: updated \'+new Date().toLocaleTimeString();\n  }catch(e){\n    document.getElementById(\'livestatus\').textContent=\'live weather: refresh failed (showing last data)\';\n  }\n}\nfunction recolor(){\n  Object.values(D.counties).forEach(c=>{ c._live=c.opportunity*(c.lift||1)*D.hours*c.incBoost; });\n  const mx=Math.max(...Object.values(D.counties).map(c=>c._live));\n  Object.entries(D.counties).forEach(([n,c])=>{c.risk=100*c._live/mx; if(c.fips&&byFips[c.fips])byFips[c.fips].risk=c.risk;});\n  render();\n}\nrefreshWeather();\nsetInterval(refreshWeather, 15*60*1000);\n\n// ---------- LIVE MODE: re-pull incidents from the newest pipeline build ----\n// Same-origin fetch of live_markers.json (republished by every Actions run;\n// no API keys in the page). Freshness is bounded by the pipeline cadence.\nasync function refreshIncidents(){\n  try{\n    const r=await fetch(\'live_markers.json?ts=\'+Date.now(),{cache:\'no-store\'});\n    if(!r.ok) return;\n    const j=await r.json();\n    if(!j || j.generated===D.generated) return; // same build, nothing new\n    D.live=j.live||D.live;\n    Object.entries(j.counties||{}).forEach(([n,u])=>{\n      const c=D.counties[n];\n      if(c){c.events=u.events;c.incBoost=u.incBoost;c.trailing=u.trailing;}\n    });\n    D.generated=j.generated;\n    drawLive(); recolor();\n    document.getElementById(\'incstatus\').textContent=\n      \'incidents: pipeline run of \'+String(j.generated).replace(\'T\',\' \').slice(0,16);\n  }catch(e){/* offline / local file view — keep build-time data */}\n}\nsetInterval(refreshIncidents, 5*60*1000);\n</script></body></html>\n"""\n'
_FILES['deploy.py'] = '"""\nTranslate county risk into an ad plan (v2).\n\nOutputs:\n  budget_shares : metro-level blend of static Phase 2 shares and live\n                  county risk (counties roll up via COUNTY_TO_METRO;\n                  unmapped counties form the \'other_in\' bucket, reported\n                  but excluded from the Phase 2 blend).\n  county_targets: ranked county list for Google Ads county location\n                  targeting, with suggested within-budget weight.\n  cluster_mix   : per-metro keyword-cluster mix from Phase 2 efficiency.\n  dayparting    : high-risk hours per metro with suggested bid adjustments.\n"""\nfrom __future__ import annotations\n\nimport pandas as pd\n\nimport config\n\n\ndef load_phase2():\n    prio = pd.read_csv(config.PHASE2_PRIORITY_CSV)\n    eff = pd.read_csv(config.PHASE2_EFFICIENCY_CSV)\n    return prio, eff\n\n\ndef budget_shares(county_scores: pd.DataFrame) -> pd.DataFrame:\n    live = county_scores.groupby("metro")["risk_score"].sum()\n    live_metros = live.drop(index="other_in", errors="ignore")\n    live_metros = live_metros / live_metros.sum()\n    w = config.DYNAMIC_WEIGHT\n    rows = []\n    for metro, static in config.STATIC_SHARES.items():\n        dyn = float(live_metros.get(metro, 0))\n        rows.append({"metro": metro, "static_share": static,\n                     "live_risk_share": round(dyn, 3),\n                     "blended_share": (1 - w) * static + w * dyn})\n    df = pd.DataFrame(rows)\n    df["blended_share"] = (df["blended_share"]\n                           / df["blended_share"].sum()).round(3)\n    df["shift_vs_static"] = (df["blended_share"]\n                             - df["static_share"]).round(3)\n    return df.sort_values("blended_share", ascending=False)\n\n\ndef county_targets(county_scores: pd.DataFrame, top_n: int = 30\n                   ) -> pd.DataFrame:\n    t = county_scores.head(top_n).copy()\n    t["within_plan_weight"] = (t["risk_score"]\n                               / t["risk_score"].sum()).round(3)\n    t["ads_location"] = t["county"] + " County, Indiana"\n    return t[["county", "metro", "risk_score", "weather_lift",\n              "active_events", "trailing_weight", "within_plan_weight",\n              "ads_location"]]\n\n\ndef cluster_mix(eff: pd.DataFrame) -> pd.DataFrame:\n    e = eff.copy()\n    e["mix"] = e.groupby("metro")["searches_per_1000usd"].transform(\n        lambda s: s / s.sum())\n    return e.sort_values(["metro", "mix"], ascending=[True, False])[\n        ["metro", "cluster", "monthly_searches", "est_cpc", "mix"]]\n\n\ndef daypart(county_hourly: pd.DataFrame,\n            county_scores: pd.DataFrame) -> pd.DataFrame:\n    """Opportunity-weighted metro hourly risk -> hours above the 75th\n    percentile with a +10%..+60% bid adjustment."""\n    h = county_hourly.merge(\n        county_scores[["county", "metro", "opportunity_score"]],\n        on="county", how="left")\n    h["w_risk"] = h["hour_risk"] * h["opportunity_score"]\n    mh = (h.groupby(["metro", "time"])["w_risk"].sum()\n          .rename("hour_risk").reset_index())\n    out = []\n    for metro, grp in mh.groupby("metro"):\n        q75 = grp["hour_risk"].quantile(0.75)\n        med = max(grp["hour_risk"].median(), 1e-9)\n        hot = grp[grp["hour_risk"] >= q75].copy()\n        hot["bid_adj_pct"] = ((hot["hour_risk"] / med - 1) * 100\n                              ).clip(10, 60).round(0)\n        out.append(hot)\n    return pd.concat(out).sort_values(["metro", "time"])\n'
for _p, _t in _FILES.items():
    with open(_p, 'w') as _f:
        _f.write(_t)

# Modules are now guaranteed-correct on disk; import and run.
import importlib, sys
for _m in ('config','sources','memory','model','geo','mapbuild','deploy'):
    if _m in sys.modules:
        importlib.reload(sys.modules[_m])
import config, deploy, geo, mapbuild, memory, model, sources

def run(hours: int = 48, outdir: str = "output"):
    os.makedirs(outdir, exist_ok=True)
    print("Loading county opportunity table ...")
    counties = sources.load_counties()
    print("Fetching weather for %d dynamic counties ..." % config.DYNAMIC_N)
    weather = sources.fetch_weather(hours)
    print("Fetching county boundaries ...")
    geodata = geo.fetch_indiana_geojson()
    print("Fetching live incidents ...")
    incidents, live_events = sources.fetch_incidents(
        geodata, list(counties["county"]))
    hist = memory.load()
    hist = memory.update(hist, live_events)
    memory.save(hist)
    live_ids = {memory.event_id(e) for e in live_events}
    trail = memory.trailing(hist, live_ids)
    trailing_scores = memory.county_trailing_scores(trail)
    if trail:
        print(f"  {len(trail)} trailing incident(s) in memory "
              f"(top: {trail[0]['type']} in {trail[0]['county']}, "
              f"{trail[0]['age_h']:.0f}h ago, importance "
              f"{trail[0]['importance']:.2f})")

    scores, hourly = model.score(counties, weather, incidents, hours,
                                 trailing_scores)
    print("Fetching commuter origin-destination data (Census LODES; "
          "~1-2 min first run) ...")
    flows = geo.fetch_flows()
    origins = geo.top_origins(flows, geodata)
    prio, eff = deploy.load_phase2()
    shares = deploy.budget_shares(scores)
    targets = deploy.county_targets(scores)
    mix = deploy.cluster_mix(eff)
    parts = deploy.daypart(hourly, scores)

    scores.to_csv(f"{outdir}/county_risk.csv", index=False)
    targets.to_csv(f"{outdir}/county_targets.csv", index=False)
    shares.to_csv(f"{outdir}/budget_shares.csv", index=False)
    mix.to_csv(f"{outdir}/cluster_mix.csv", index=False)
    parts.to_csv(f"{outdir}/dayparting.csv", index=False)
    # Only crashes get markers (filtered in-page via MAP_MIN_MARKER_WEIGHT);
    # every event still shades its county and is itemized in its panel.
    mapbuild.build_map(scores, geodata, origins, f"{outdir}/risk_map.html",
                       hours, live=live_events)

    print("\n=== Blended budget shares (next %d h) ===" % hours)
    print(shares.to_string(index=False))
    print("\n=== Top 10 counties ===")
    print(targets.head(10).to_string(index=False))
    print(f"\nOutputs written to ./{outdir} (open risk_map.html).")
    return scores, shares, targets, mix, parts


if __name__ == "__main__":
    ap = argparse.ArgumentParser()
    ap.add_argument("--hours", type=int, default=48)
    args = ap.parse_args()
    run(args.hours)
